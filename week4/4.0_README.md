# Week 4 — Motion Sensing and the IMU

**Lecture:** Oct 1 · **Lab:** Sept 28

## Lecture Topic

Motion sensing and the IMU: Bar and Switch widgets — the second of six lectures
(Weeks 3–8) that each pair one Core S3 hardware capability with the M5UI widget used to work
with it.

## This Week's Materials

| File | Covers |
|---|---|
| `4_1_motion_sensing_and_the_IMU.md` | What the IMU measures and reading it with `M5.Imu.getAccel()`; the clamp-and-scale pattern for turning a raw accelerometer axis into a bounded display value; `M5Bar` (read-only, updated from `loop()`, no `VALUE_CHANGED` event); `M5Switch` (event-driven, same build/handler/dispatch shape as every widget so far); mixing poll-driven and event-driven updates in one program; a full worked example (Tilt Monitor); the header-block requirement that now applies from Lab 3 onward |
| `PRG455_Lab4_IMU_Tilt_Calibration_Panel.md` | This week's lab specification: a touchscreen tilt diagnostic panel using a continuously IMU-driven bar, a live value label, and a calibration switch that changes the bar's sensitivity |

## Lab Activity — Lab 4: IMU Tilt & Calibration Panel

Build a single-screen M5UI application on the Core S3 with a tilt bar that continuously reflects
the device's X-axis accelerometer reading (via the required clamp-and-scale formula), a live
value label updating in step with the bar, and a calibration switch that toggles between Normal
and Sensitive interpretation of the tilt. No buttons — the bar and label update on their own,
every pass through `loop()`, with the switch changing behaviour immediately on toggle.

## Evaluation

| Component | Weight |
|---|---|
| Lab 4 | 5.5% |

## Before this lab

Read `4_1_motion_sensing_and_the_IMU.md` in full, and be comfortable with the difference between
poll-driven widget updates (bar, run every `loop()` pass) and event-driven widget updates
(switch, run only on `VALUE_CHANGED`) before opening Claude. Review the required 10-field header
block format in Lab 4's Code Requirements — it applies to this submission and every one after it.

## Self-check before submission

- Required header block present, complete, no placeholders
- Title visible, no overlap
- Tilt bar continuously reflects the IMU reading via the exact clamp-and-scale formula
- Live value label updates in step with the bar, three decimal places, correct sign
- Calibration switch toggles Normal/Sensitive immediately, mode label updates to match
- No blocking calls in `loop()`; bar and switch both stay responsive at the same time
- `PROMPTS.md` present and current if AI assistance was used
