# Week 5 — The Camera and Visual Feedback

**Lecture:** Oct 8 · **Lab:** Oct 5

## Lecture Topic

The camera and visual feedback: the Image widget — the third of six lectures
(Weeks 3–8) that each pair one Core S3 hardware capability with the M5UI
widget used to work with it.

## This Week's Materials

| File | Covers |
|---|---|
| `5_1_the_camera_and_the_Image_widget.md` | Why live camera preview and `M5Image` use two separate display paths (`M5.Lcd.show()` vs. an M5UI widget); initializing the camera with `camera.init()`; capturing frames with `camera.snapshot()`; converting a frame to JPEG with `to_jpeg()` and saving it to flash; the `M5Image` API (`set_image`, `set_pos`, `set_rotation`, `set_scale`) and its must-exist-at-construction-time gotcha; a full worked example (live preview + Capture + redisplay) with a two-axis overlap check |
| `PRG455_Lab5_Camera_Snapshot_Panel.md` | This week's lab specification: a live camera preview, a Capture button that saves a JPEG and redisplays it in an `M5Image` widget, and a Clear button that reverts to a placeholder — including the mandatory 10-field header block |

## Lab Activity — Lab 5: Camera Snapshot Panel

Build a single-screen M5UI application on the Core S3 with a live camera
preview drawn via `M5.Lcd.show()`, a Capture button that saves the current
frame as a JPEG to flash and displays it in an `M5Image` widget, a Clear
button that reverts the display to a placeholder image, and a status label
that accurately reflects whether a photo has been captured. Touch-only
operation, no overlap between the live preview and any other widget on
either axis.

## Evaluation

| Component | Weight |
|---|---|
| Lab 5 | 5.5% |

## Before this lab

Read `5_1_the_camera_and_the_Image_widget.md` in full, particularly §1 on
why the live preview and `M5Image` use different display paths — this is
the idea the whole lab's layout depends on. Review the header block
convention from Lab 4 if you need a refresher; it's required again this
week and graded the same way (flat −10 penalty, not a rubric line item).

## Self-check before submission

- Required header block present, complete, and free of placeholder text
- Title visible, no overlap with any other element
- Live preview updates continuously and fills at least 280×150px
- Capture saves a fresh frame (not stale) as JPEG and updates the
  `M5Image` widget within about a second
- `M5Image` widget constructed against a valid placeholder path, never
  overlaps the live preview rectangle on either axis
- Clear reverts the image and status label in one tap, without restarting
- Status label always accurately reflects captured/not-captured state
- Program survives 60+ seconds of continuous Run Once operation, including
  multiple Capture/Clear cycles
- `PROMPTS.md` present and current if AI assistance was used
